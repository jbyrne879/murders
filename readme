We analyze US gun murder stata collected by the FBI

download-data.R - downloads csv file to data directory

wrangle-data.R - creates a derived data set and save as R objet in rda directory

analysis.R - generates a plot and save it to teh figs directory 